"""dicti, local Linux dictation daemon + tray indicator."""

# Single source of truth for the version. pyproject.toml derives its version
# from this attribute (see [tool.setuptools.dynamic]), so bump it here only.
__version__ = "0.3.7"
